<source>
  @type tail
  tag docker.*
  path /var/log/docker/*.log
  pos_file /fluentd/log/docker.pos
  format json
</source>

<match **>
  @type elasticsearch
  host elasticsearch
  port 9200
  logstash_format true
  flush_interval 5s
</match>
